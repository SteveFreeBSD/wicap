# Contributing

The canonical contributing guide lives in `docs/CONTRIBUTING.md`.

Quick checks before you open a PR:

- `./scripts/review_gate.sh`
- `python -m replay_driver --batch tests/fixtures/manifest.json` (when parsing/detection changes)

If you're new here, start at `docs/INDEX.md`.
