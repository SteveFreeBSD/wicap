# BLE Soak Report (nRF52840)

Date: 2026-01-30  
Duration: 10 minutes  
Scope: BLE capture backend (tshark + Nordic extcap)  
Host: `artemis` (Artix Linux)  
Operator: `steve`

---

## Summary

The BLE capture backend ran continuously for 10 minutes and successfully
captured live advertising traffic. Events were parsed in real time and a
PCAPNG file was written without errors.

**Verdict:** PASS ✅

---

## Environment

- Interface: `/dev/ttyACM0-4.6`
- Extcap path: `tools/bluetooth/extcap` (via `WICAP_BT_EXTCAP_DIR` default)
- User groups: `uucp`, `wireshark` (device access OK)

---

## Results

- Events captured: **9,619**
- Unique BLE addresses: **1,089**
- RSSI min / max / avg: **-93 / -50 / -77.41 dBm**
- Output PCAP: `captures_verify/ble_soak/ble_soak_1769743341.pcapng`
- Output size: **935,880 bytes**

---

## Command (Repro)

```bash
PYTHONPATH=. python - <<'PY'
import time
from pathlib import Path
from src.wicap.core.capture.bluetooth_backend import BluetoothCaptureBackend

backend = BluetoothCaptureBackend("/dev/ttyACM0-4.6", Path("captures_verify/ble_soak"))
count = 0
def cb(_): 
    global count
    count += 1

backend.start_capture("ble_soak", callback=cb)
time.sleep(600)
backend.stop_capture()
print("events:", count)
PY
```

---

## Notes

- Auto‑detection of valid tshark fields prevented invalid‑field crashes.
- No capture interruptions observed; process remained healthy for full duration.
